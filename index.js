let animals = []

function setup(){
        createCanvas(800, 800);
        strokeWeight(1);
        background(0);
        rectMode(CENTER);
        stroke(255);
        fill(0);
}

function draw(){
    background(0)
    fill(255)
    textSize(32)
    text("Hello World", 10, 50)
}

function populate(){

}